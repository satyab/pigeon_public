<div class='container'>
  <h1> Name : <%= campaign.name %> </h1>
  <h2> Impressions : <%= campaign.limit - campaign.remainingLimit %> </h2>
  <h3> Categories : </h3>
  <h4>
    <% for (i in categories) {%>
    <span class="label label-primary"><%= categories[i].category %></span>
    <% } %>
  </h4>
</div>
